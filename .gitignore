# Runtime state, regenerated per machine, never part of the release
config.json
Logs/
*.log

# Build output. Run build.ps1 to regenerate rather than committing
# compiled binaries to source control. Release zips get attached to
# GitHub Releases instead, not committed either.
dist/
*.zip

# Editor / OS cruft
.vs/
.vscode/
Thumbs.db
desktop.ini
